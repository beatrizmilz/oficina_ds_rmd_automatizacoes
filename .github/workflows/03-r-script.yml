on:
  workflow_dispatch:
    branches: [main]
    
    
name: 03-run-r-script  

jobs:
  run-r-script:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: r-lib/actions/setup-r@v2
      
      - name: print-message
        run: |
        Rscript ghactions/03-r-script.R
        